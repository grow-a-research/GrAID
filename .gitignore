.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Hugging Face / model caches (keep out of repo)
.cache/
**/.cache/

# Local DB + uploaded submission files
data/

# Local outputs
procPapers/
**/procPapers/
*.png
*.jpg
*.jpeg

# Frontend
frontend/node_modules/
frontend/dist/

# OS/editor
.DS_Store
Thumbs.db
.vscode/

# DREsS dataset is restricted-access (no personal sharing per its README) —
# this repo is public, so none of its essay text/scores may be committed.
# Any file that reproduces DREsS essay content goes here, not just these.
classification_model/dress_shared_subset_candidates.csv
classification_model/dress_shared_subset_candidates.pdf
classification_model/dress_shared_scores_template.csv
classification_model/dress_shared_scores_entry.xlsx
classification_model/dress_data/

# node_modules anywhere (not just frontend)
node_modules/

# Local OCR scratch dumps (contain real student essay text)
content_*.txt
whole_*.txt
box-crop_*.txt

# Local benchmark output (contains Vast.ai host + student answers)
bench_*.json

# node_modules anywhere (not just frontend)
node_modules/

# Local OCR scratch dumps (contain real student essay text)
content_*.txt
whole_*.txt
box-crop_*.txt

# Local benchmark output (contains Vast.ai host + student answers)
bench_*.json
